/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Images: only local /logo.png used; add remotePatterns if you use external images
}

module.exports = nextConfig

